// TODO: Write your gambling application here.
const allScratchys = document.querySelectorAll('#scratchcards');
allScratchys.addEventListener('click', (event) = => {
  const moneyBalance = document.querySelector('#balance');
  moneyBalance -= 10
}
)
